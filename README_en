When you insert USB memory into the 3D printer, it overwrites or deletes the g code of the file with the same name.

Function
######

1. Copy - Insert a USB flash containing one file or multiple "gcode" files
   Copies to the print folder
   
2. Move - Insert an empty USB flash and all files will be moved.
   "gcode" for folder to print
   
   The folder you want to print is set in the "mountcopy" file.
   
   # For raspberry pie
   #DESTINATION="/home/pi/printer_data/gcodes"
   
   # When using the BIQU CB1 module
   대상="/home/biqu/printer_data/gcodes"

Installation
######

Connect to SSH terminal, will remove existing algorithms and install new ones.
If you don't have an existing algorithm, you can exclude up to step 5.

1. Remove previously used Udev rules.
   sudo rm /etc/udev/rules.d/99-mountcopy.rules

2. Delete the helper script.
   sudo rm /usr/bin/mountcopy

3. Enter when a password request is received
   
4. Load the UDEV configuration.
   sudo udevadm control --reload-rules && sudo udevadm trigger

5. Remove the previous algorithm.
   <Enter the 3 lines below at once> 
   sudo rm -rf /home/biqu/mount_copy
   mkdir -p /home/biqu/mount_copy
  cd /home/biqu/mount_copy

6. Install a new algorithm.
   cd
   git clone https://github.com/MOMENT3D/mount_copy_v2.git
   cd /home/biqu/mount_copy_v2
   chmod +x *.sh
   ./install.sh

Removal
######
   cd
   cd /home/biqu/mount_copy_v2
   chmod +x *.sh
   ./uninstall.sh
